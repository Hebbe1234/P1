#ifndef STRUCTS
#define STRUCTS

#include "../headers/structs.h"

#endif
#include <stdio.h>
#include <stdlib.h>
#include <math.h>



void wait_time(transition_system *t) {
    passenger p;
    int i;

    for(i = 0; p.wait_time > 0; i++) {
        if(p.wait_time > 0) {
            p.wait_time = 1;
        }
    }

}
